{
  "name": "screencap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "_test": "mocha --bail --no-timeout --watch-files 'test/*.js'",
    "test:gui": "mocha --bail --no-timeout --watch-files 'test/*.js' test/gui/*.spec.js",
    "test:cli": "mocha --bail --no-timeout --watch-files 'test/*.js' test/cli/*.spec.js",
    "test:mocha": "mocha"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "chai": "^4.2.0",
    "chromedriver": "^79.0.0",
    "memory-streams": "^0.1.3",
    "mocha": "^7.0.1",
    "selenium-webdriver": "^4.0.0-alpha.5"
  }
}
